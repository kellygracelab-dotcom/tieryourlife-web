{
  "name": "tieryourlife-web-functions",
  "description": "Server side of the TierYourLife website: keeps the rankings visitors make and serves the pages that need OpenGraph tags",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc -p tsconfig.test.json && node --test \"lib-test/test/**/*.test.js\"",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "@resvg/resvg-wasm": "^2.6.2",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.1.0",
    "satori": "^0.33.4"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
